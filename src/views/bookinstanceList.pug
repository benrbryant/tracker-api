extends layout 

block content 
    h1= title 

    ul 
        each copy in bookinstanceList 
            li 
                a(href=copy.url) #{copy.book.title} : #{copy.imprint} -
                span #{copy.status}
        else
            li There are no book copies in this library.